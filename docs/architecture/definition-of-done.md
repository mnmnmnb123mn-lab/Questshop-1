# Definition of done

Source work is complete only when Node 22.22 SQLite checks, lint, tests with no skips, coverage at least 70%, load test,
dependency audit, Docker build and diff check pass. The release label remains **implemented-but-unverified** until a
single Git SHA passes persistent `/data`, restart, redeploy, backup restore, Discord desktop/mobile, TrueMoney and
Owner UAT.
