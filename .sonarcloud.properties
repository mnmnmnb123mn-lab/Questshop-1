# These are immutable PostgreSQL migrations with runtime checksum validation
# and PostgreSQL integration coverage. Sonar Automatic Analysis classifies
# .sql files as PL/SQL and raises rules that do not model PostgreSQL DDL.
sonar.exclusions=migrations/**
sonar.javascript.lcov.reportPaths=coverage/lcov.info
